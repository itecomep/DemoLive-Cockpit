import { OtpInputKeysPipe } from './otp-input-keys.pipe';

describe('OtpInputKeysPipe', () => {
  it('create an instance', () => {
    const pipe = new OtpInputKeysPipe();
    expect(pipe).toBeTruthy();
  });
});
