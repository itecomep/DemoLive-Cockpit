import { McvPopoverDirective } from './mcv-popover.directive';

describe('McvPopoverDirective', () => {
  it('should create an instance', () => {
    const directive = new McvPopoverDirective();
    expect(directive).toBeTruthy();
  });
});
